import {
  describe,
  it,
} from 'mocha';
import {
  expect,
} from 'chai';

describe('MessageDeSerializer', function describeMessageSerializer() {
  it('should just succeed', async function itShouldSucceed() {
    expect(true).to.be.true;
  });
});
