import {
  describe,
  it,
} from 'mocha';
import {
  expect,
} from 'chai';

describe('message-definitions', function describeMessageDefinitions() {
  it('should do nothing', async function doNothing() {
    expect(true).to.be.true;
  });
});
